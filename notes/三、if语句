一、条件测试
1、每条if语句的核心都是一个值为True或False的表达式，这种表达式被称为条件测试。
（1）Python用 == 来判断两边的内容是否相等;用 != 来判断两边的内容不相等
（2）比较数字：同java，分别使用==、!=、>、>=、<、<=
2、多个条件
使用and或or检查多个条件
3、列表是否包含特定元素
in和not in
二、if语句
1、if
仅有一种情况
2、if-else
两种情况
3、if-elif-else
超过两种情况

